services:
  - type: web
    name: seo-quote-tool
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: DFS_LOGIN
        sync: false
      - key: DFS_PASSWORD
        sync: false
