Add this line to requirements.txt before deploying:

    zipcodes>=3.0.0

It bundles US city coordinates (29,788 city+state pairs, ~1.3 MB, no API
key, no network calls at runtime). Geographic market grouping needs it.

If the line is missing the app still runs — grouping falls back to the
older volume-based method and the panel says which one it used.
